[package]
name = "tpt-opt-heuristic"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
description = "Metaheuristic optimization: simulated annealing, genetic algorithms, tabu search, and particle swarm optimization, with deterministic seeding."
keywords = ["metaheuristic", "genetic-algorithm", "simulated-annealing", "optimization", "swarm"]
categories = ["science::math", "algorithms"]
documentation = "https://docs.rs/tpt-opt-heuristic"

[dependencies]
    tpt-opt-core = { workspace = true }
    tpt-math-prob = { workspace = true }

[features]
default = ["std"]
std = []
