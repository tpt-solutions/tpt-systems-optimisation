[package]
name = "tpt-opt-cp"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
description = "Constraint-programming engine: domain propagation, global constraints, and first-fail backtracking search."
keywords = ["constraint-programming", "scheduling", "propagation", "optimization", "combinatorial"]
categories = ["science::math", "algorithms"]
documentation = "https://docs.rs/tpt-opt-cp"

[dependencies]
    tpt-opt-core = { workspace = true }

[features]
default = ["std"]
std = []
